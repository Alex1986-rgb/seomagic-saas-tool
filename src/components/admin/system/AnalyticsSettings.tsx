import React from 'react';
import NotCollectedNotice from '@/components/admin/NotCollectedNotice';

/**
 * Настройки аналитики.
 *
 * Здесь был переключатель Google Analytics с подставленным несуществующим
 * счётчиком UA-123456789-1, «собственная аналитика» со сроком хранения и
 * набором собираемых данных и кнопка «Сохранить», которая только печатала всё
 * в консоль. Ни один счётчик на сайт не ставился, настройки сбрасывались при
 * перезагрузке.
 */
const AnalyticsSettings: React.FC = () => (
  <div className="p-6">
    <NotCollectedNotice
      title="Подключение внешней аналитики из админки не настраивается"
      description="Поля и переключатели, которые здесь были, ничего не сохраняли и убраны. Как устроено на самом деле:"
      items={[
        "счётчика посещаемости (Яндекс Метрики, Google Analytics) на сайте нет",
        "чтобы его поставить, код счётчика добавляют в сборку сайта — через админку это не делается",
        "срока хранения и набора собираемых данных здесь нет: собственной аналитики у сервиса нет",
      ]}
    />
  </div>
);

export default AnalyticsSettings;
