import React from 'react';
import NotCollectedNotice from '@/components/admin/NotCollectedNotice';

/**
 * Настройки безопасности.
 *
 * Здесь были переключатели двухфакторной аутентификации, ограничения по IP и
 * защиты от подбора паролей, поле «Глобальный пароль администратора» и кнопка
 * «Сохранить», которая только печатала настройки в консоль. Ни одна из этих
 * настроек в сервисе не реализована: включённый переключатель ничего не
 * защищал, а после перезагрузки всё сбрасывалось.
 */
const SecuritySettings: React.FC = () => (
  <div className="p-6">
    <NotCollectedNotice
      title="Защита входа из админки не настраивается"
      description="Переключатели и поле пароля, которые здесь были, ничего не сохраняли и убраны. Как устроено на самом деле:"
      items={[
        "вход и пароли обслуживает Supabase Auth — ограничения частоты входа и двухфакторная аутентификация настраиваются в панели Supabase, раздел Authentication",
        "общего «пароля администратора» нет: права дают роли в таблице user_roles",
        "ограничения доступа к админке по IP-адресам в сервисе нет",
        "смены пароля в интерфейсе сервиса пока нет — письмо для сброса пароля отправляется из панели Supabase, раздел Authentication → Users",
      ]}
    />
  </div>
);

export default SecuritySettings;
