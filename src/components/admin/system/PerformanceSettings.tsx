import React from 'react';
import NotCollectedNotice from '@/components/admin/NotCollectedNotice';

/**
 * Настройки производительности.
 *
 * Здесь были переключатели кеширования, сжатия и оптимизации изображений,
 * лимиты запросов и пороги оповещений по CPU, памяти и диску, а кнопка
 * «Сохранить» только печатала всё это в консоль. Ни одна настройка ни на что не
 * влияла, а показаний CPU, памяти и диска сервис не снимает.
 */
const PerformanceSettings: React.FC = () => (
  <div className="p-6">
    <NotCollectedNotice
      title="Параметры производительности из админки не настраиваются"
      description="Переключатели, лимиты и пороги, которые здесь были, ничего не сохраняли и убраны. Как устроено на самом деле:"
      items={[
        "сайт — статическая сборка, кеширование и сжатие отдаёт хостинг, на котором она опубликована",
        "серверная часть — edge-функции и база Supabase; их лимиты задаются тарифом и настройками проекта в панели Supabase",
        "журнал вызовов серверных функций (неполный: пишут не все функции) — на странице мониторинга",
      ]}
    />
  </div>
);

export default PerformanceSettings;
