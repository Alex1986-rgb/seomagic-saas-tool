import React from 'react';
import NotCollectedNotice from '@/components/admin/NotCollectedNotice';

/**
 * Настройки системных уведомлений.
 *
 * Здесь были переключатели email-, SMS- и Slack-оповещений, получатель
 * admin@example.com, типы событий вроде «Нехватка места на диске» и «Высокая
 * загрузка CPU» и кнопка «Сохранить», которая только печатала настройки в
 * консоль. После перезагрузки всё сбрасывалось, оповещения никуда не уходили,
 * а метрик диска и CPU сервис вообще не собирает.
 */
const NotificationsSettings: React.FC = () => (
  <div className="p-6">
    <NotCollectedNotice
      title="Каналы оповещений не подключены"
      description="Переключатели и поля, которые здесь были, ничего не сохраняли и убраны. Что есть на самом деле:"
      items={[
        // Раздел «Уведомления» админки показывает только уведомления самого
        // администратора, клиентские там не видны.
        "уведомления записываются в базу и видны получателю в личном кабинете; в разделе «Уведомления» админки — только уведомления самого администратора",
        "рассылки оповещений администратору по почте, SMS или в Slack нет",
        "оповещений о нагрузке сервера нет — сервис не собирает метрики CPU, памяти и диска",
      ]}
    />
  </div>
);

export default NotificationsSettings;
