import React from 'react';
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Info } from 'lucide-react';

/**
 * Настройки приёма платежей.
 *
 * Здесь были поля для ключа Stripe, PayPal Client ID, валюты и ставки налога,
 * кнопка «Сохранить», которая ждала 0,8 секунды и отвечала «Настройки платежей
 * сохранены», и кнопка проверки, которая через 1,5 секунды рапортовала
 * «Подключение успешно. Все платёжные системы работают корректно». Ни одного
 * запроса при этом не уходило: приём платежей в сервисе не подключён, а
 * введённые ключи нигде не сохранялись.
 *
 * Хранить ключи платёжных систем в полях админки и в базе нельзя — им место в
 * секретах проекта. Когда приём оплаты будет подключён, здесь появятся
 * настройки, которые действительно сохраняются.
 */
const PaymentSettings: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="flex items-center gap-2">
          <Info className="h-5 w-5 text-muted-foreground" />
          Приём платежей не подключён
        </CardTitle>
        <CardDescription>
          Оплата на сайте сейчас не принимается: заявки на счёт приходят через форму и
          раздел «Заявки».
        </CardDescription>
      </CardHeader>
      <CardContent className="space-y-3 text-sm text-muted-foreground">
        <p>
          Чтобы включить оплату, нужен договор с платёжным сервисом (ЮKassa, Stripe и т. п.)
          и серверная часть: ключи платёжной системы хранятся в секретах проекта, а не в
          настройках сайта, и карту принимает сам платёжный сервис на своей стороне.
        </p>
        <p>
          До этого момента раздел оставлен пустым намеренно — чтобы никто не вводил сюда
          настоящие ключи, считая, что они сохраняются.
        </p>
      </CardContent>
    </Card>
  );
};

export default PaymentSettings;
