import React from "react";
import NotCollectedNotice from "@/components/admin/NotCollectedNotice";
import PageSeo from '@/components/seo/PageSeo';

/**
 * API-ключи и доступ.
 *
 * Здесь был список «активных ключей» sk_live_123...abc и sk_test_456...xyz с
 * датами создания, кнопка «Создать ключ», которая просто закрывала форму, кнопки
 * «Удалить» без обработчика и обещания «все ключи зашифрованы», «автоматическая
 * ротация», «ограничение по IP». Выдачи ключей в сервисе нет, всё было нарисовано.
 */
const ApiKeysPage = () => (
  <div className="container mx-auto px-4 py-8 max-w-3xl">
    <PageSeo
      title="API-ключи: выдача ключей внешним интеграциям не подключена"
      description="Сервис пока не выдаёт API-ключи внешним системам; ключи поставщиков хранятся в секретах проекта на сервере и в админку не выводятся."
      noindex
    />
    <h2 className="text-2xl font-bold mb-3">API ключи и доступ</h2>

    <NotCollectedNotice
      title="Выдачи API-ключей в сервисе нет"
      description="Ключи и кнопки создания, копирования и удаления, которые здесь были, ни к чему не подключались. Как устроено на самом деле:"
      items={[
        "внешнего API для клиентов у сервиса нет, выдавать ключи некому",
        "ключи поставщиков (языковая модель, выдача позиций, почта) лежат в секретах проекта Supabase и в браузер не попадают",
        "добавить или сменить такой ключ можно только в панели Supabase, раздел Edge Functions → Secrets",
      ]}
    />
  </div>
);

export default ApiKeysPage;
