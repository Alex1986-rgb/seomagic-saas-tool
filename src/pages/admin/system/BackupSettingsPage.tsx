import React from "react";
import NotCollectedNotice from "@/components/admin/NotCollectedNotice";
import PageSeo from '@/components/seo/PageSeo';

/**
 * Резервное копирование.
 *
 * Здесь была кнопка «Создать копию», которая через две секунды таймера
 * показывала «Резервная копия успешно создана» и подставляла текущее время как
 * дату последней копии, список «копий» по 156 MB с кнопками «Скачать» и обещание
 * «Ежедневное резервное копирование в 03:00». Ни одного запроса при этом не
 * уходило — копии не существовало, и восстанавливаться было бы не из чего.
 *
 * Из админки бэкапы не делаются. Копии базы — забота Supabase, и что именно
 * доступно, зависит от тарифа проекта; сюда эти данные не выводятся.
 */
const BackupSettingsPage = () => (
  <div className="container mx-auto px-4 py-8 max-w-3xl">
    <PageSeo
      title="Резервное копирование: из админки не управляется"
      description="Резервные копии базы из админки не создаются и не восстанавливаются: они делаются на стороне Supabase в зависимости от тарифа проекта."
      noindex
    />
    <h2 className="text-2xl font-bold mb-3">Резервное копирование</h2>

    <NotCollectedNotice
      title="Из админки резервные копии не создаются"
      description="Кнопки создания и скачивания копий, история бэкапов и расписание, которые здесь были, ничего не делали. На самом деле:"
      items={[
        "копии базы делает Supabase; что доступно (ежедневные копии, восстановление на момент времени), зависит от тарифа проекта",
        "проверить наличие копий и восстановиться можно только в панели Supabase, раздел Database → Backups",
        "перед рискованной операцией с базой убедитесь в свежей копии там, а не на этой странице",
      ]}
    />
  </div>
);

export default BackupSettingsPage;
