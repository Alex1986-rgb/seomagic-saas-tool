
import React from 'react';
import { AdminFormContainer, AdminFormTitle } from '@/components/admin/AdminFormStyles';
import ProxyManager from '@/components/admin/proxies/ProxyManager';
import PageSeo from '@/components/seo/PageSeo';
import NotCollectedNotice from '@/components/admin/NotCollectedNotice';

/**
 * Прокси.
 *
 * Раньше «Собрать прокси» генерировал случайные адреса и часть из них сразу
 * помечал рабочими, проверка адресов выдавала случайные коды ответа, а пинг
 * всегда «успешно». Теперь сбор, проверка адресов и пинг делают настоящие
 * запросы. Но всё это работает из браузера администратора, и у браузера
 * есть пределы, о которых страница говорит прямо, — иначе пустые или
 * ошибочные результаты выглядели бы как поломка.
 */
const AdminProxiesPage: React.FC = () => {
  return (
    <AdminFormContainer>
      <PageSeo
        title="Прокси-серверы: сбор списков и импорт адресов"
        description="Список прокси в браузере администратора: сбор адресов из открытых источников, импорт и экспорт. Работоспособность прокси из браузера не проверяется."
        noindex
      />
      <AdminFormTitle>Управление прокси</AdminFormTitle>
      <NotCollectedNotice
        className="mb-6"
        title="Что этот раздел может на самом деле"
        description="Раздел работает прямо в браузере и хранит список только в нём. Отсюда следуют ограничения:"
        items={[
          'сбор скачивает списки из источников; многие сайты не разрешают браузеру читать их с чужого домена (CORS) — такие источники завершаются ошибкой, и это не сбой раздела',
          'найденные адреса никто не проверял: у них статус «testing», «активными» они не становятся',
          'проверить прокси из браузера нельзя — браузер не умеет отправить запрос через прокси, поэтому «Проверить прокси» статус не меняет',
          'пинг страниц и проверка адресов идут напрямую, без прокси; сервисы, закрытые для браузера, вернут ошибку',
          'позиции сайтов проверяются на сервере через внешнего поставщика — этот список на них не влияет',
        ]}
      />
      <ProxyManager />
    </AdminFormContainer>
  );
};

export default AdminProxiesPage;
