/** Текст без строчной разметки статей (**…**, `…`, [текст](адрес)) — для счёта знаков и JSON-LD. */
export const plainText = (s: string) => s.replace(/\[([^\]]+)\]\([^)]+\)/g, '$1').replace(/[*`]/g, '');
