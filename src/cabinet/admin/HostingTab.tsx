import React from 'react';
import { Lead } from './shared';
import { NotConnected } from '../ui';

/**
 * Хостинг — макет, строки 2140–2311.
 *
 * В макете Beget по API создаёт поддомены превью, выпускает SSL и заливает исправленную копию.
 * Ничего из этого в продукте нет: сборка исправленной копии сайта не реализована, ключей хостинга
 * платформа не хранит. Раздел старой админки (HostingPage.tsx) пуст намеренно — прежняя версия
 * складывала пароли от чужих серверов в localStorage открытым текстом. Поэтому здесь нет ни формы
 * доступов, ни таблицы поддоменов, ни «журнала API»: только список того, что нужно построить.
 */
const HostingTab: React.FC = () => (
  <div style={{ display: 'grid', gap: 'var(--space-6)' }}>
    <Lead>
      Через хостинг платформа должна публиковать превью исправленного сайта на поддомене, закрытом от индексации, и
      устанавливать одобренные правки. Этот контур ещё не построен.
    </Lead>

    <NotConnected
      title="Публикация превью и установка правок не подключены"
      needs={[
        'сборка исправленной копии сайта (сейчас не реализована)',
        'интеграция с API хостинга (Beget или свой сервер превью): поддомен, SSL, загрузка файлов, удаление по сроку',
        'закрытие превью от индексации (noindex, robots.txt, пароль) и регулярная проверка, что оно закрыто',
        'хранение ключей хостинга вне браузера — в секретах функций, не в localStorage и не в открытых столбцах',
        'таблица поддоменов превью (заказ, размер, срок жизни, состояние индексации) и журнал вызовов API',
      ]}
    />
  </div>
);

export default HostingTab;
