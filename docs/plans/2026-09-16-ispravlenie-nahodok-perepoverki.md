# Исправление находок повторной проверки — 16.09.2026

Повторная проверка 237 изменённых файлов (5 областей, у каждой находки — скептик) дала
86 находок, 83 подтверждены (11 серьёзных). Полный список — `scratchpad/review.json` сессии.

## Сделано сразу

- Ночная уборка удаляла бы задачи вошедших пользователей старше 90 дней вместе с
  оптимизациями и сметами (каскад). Функция переписана: чистятся только технические следы и
  гостевые аудиты. Миграция `20260916170000_cleanup_keeps_user_data.sql` применена.

## План

1. Девять исправителей параллельно, у каждого своя область файлов, чужие не трогают:
   бэкенд; сценарий «адрес → аудит → история»; результаты и оптимизация; глубокое
   сканирование; кабинет и вход; формы админки; данные админки; SEO и сборка; публичные
   страницы и ссылки.
2. Проверка: типы, тесты, сборка — один прогон после всех правок.
3. Повторная опровергающая проверка диффа исправлений.
4. Выкатка функций и миграций, проверка вживую.
5. Выкладка фронтенда на Pages — только по явному «да»: скрипт выкладки сотрёт демо-клоны
   myarredo-by/ и rimmebel/, а без пререндера все внутренние страницы отдаются с кодом 404.
6. Отчёт.
