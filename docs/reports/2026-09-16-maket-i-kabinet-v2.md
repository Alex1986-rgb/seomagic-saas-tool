# Макет SeoMarket 2.0 и кабинет по макету — 16.09.2026

## Сделано
- **Макет опубликован:** https://alex1986-rgb.github.io/seomarket-design/ (репо `Alex1986-rgb/seomarket-design`, публичный,
  Pages из main). Оглавление + лендинг, блог, статья, отзывы, документы, лента статей, кабинет (20 экранов), 3 письма.
  Все страницы `noindex`, robots.txt закрыт, плашка «Дизайн-макет · данные условные». Внутренние заметки не выложены.
- **Шрифты:** Barlow без кириллицы → Fira Sans Condensed / Fira Sans / Fira Code (у IBM Plex Sans Condensed тоже нет базовой кириллицы); в макете и в коде (`src/design/industry.css`, @fontsource).
- **Иконки:** 221 Lucide (перо 1.5) на шести публичных страницах.
- **Фото:** 40 рамок — Unsplash с подписью автора (`photos/stock.json`); «до» — скриншот текущего сайта на Pages,
  «после» — скриншот макета; в статье две SVG-схемы с пометкой «схема, не замер».
- **Промты:** `design/seomarket-v2/photo-prompts.md` на все 44 рамки — для замены фото своей генерацией.
- **Кабинет 2.0 в коде** (`src/cabinet/`, маршруты `/app/*`, старый кабинет не тронут): стили Industry только внутри
  кабинета, 20 экранов на настоящих данных Supabase, где бэкенда нет — честное «не подключено».
  tsc чистый, тесты 169/169, сборка проходит. План — `docs/plans/2026-09-16-kabinet-po-maketu-v2.md`.

## Осталось
- В dev-режиме три экрана кабинета (Новый аудит, Позиции, Отчёты) не открываются: dev-плагин `lovable-tagger` ломает
  разбор JSX. В прод-сборке ошибок нет. Решение — отключить плагин в `vite.config.ts` или найти конструкцию-триггер.
- Кабинет проверен в браузере только без входа (пустые состояния); на живом аккаунте не смотрели.
- Бэкенд, которого не хватает: публичное чтение сметы по ссылке (`get_shared_estimate`), запрет клиенту ставить смете
  `paid`, оплата с холдом, копия сайта/превью/откат, конкуренты (сохранение выдачи), Wordstat, CWV, таблица проектов,
  2FA на уровне RLS, перегенерация `types.ts`.
- Unsplash-фото — компромисс в 13 кадрах (см. `note` в stock.json).

## Нужно от Александра
- Пополнить router.cheap (баланс $0,06) или Higgsfield (0 кредитов), если нужны свои сгенерированные фото вместо Unsplash.
- В Supabase → Auth → Redirect URLs добавить `…/app/reset*` (иначе ссылка сброса пароля уйдёт на Site URL).
- Решить, выкатывать ли кабинет `/app` на Pages вместе с сайтом.

## Файлы
`design/seomarket-v2/**`, `src/cabinet/**`, `src/App.tsx`, `package.json` (+@fontsource barlow, barlow-condensed,
roboto-condensed), `docs/plans/2026-09-16-kabinet-*.md`, этот отчёт.

## Дополнение: публичный сайт по макету (16.09.2026, вечер)
- Сайт собран в самом сервисе на Industry: главная (гостевая проверка до 10 страниц — настоящий аудит, ставки из
  `pricing_rules`, 27 проверок в 5 категориях), блог + 11 статей (`/blog/<slug>`, пререндер, sitemap), отзывы (без
  выдуманных — правила публикации), 8 документов, регистрация `/app/register`. Старые публичные адреса и старый
  кабинет переадресуются на новые. Шрифты — Fira. Плагин lovable-tagger убран (ломал dev).
- Проверено: tsc чистый, тесты 177/177, сборка + пререндер (23 адреса), 18 адресов × 1440/375 в браузере без
  падений и горизонтальной прокрутки.
- Не выложено на Pages — ждёт «да». Документы (152-ФЗ: база в eu-west-1) — отложено по решению Александра.
- Хвосты: edge-функция `blog-sitemap` отдаёт старые адреса блога; в Supabase Redirect URLs добавить `…/app`
  и `…/app/reset*`; в классификаторе 4–5 проверок почти не срабатывают.
