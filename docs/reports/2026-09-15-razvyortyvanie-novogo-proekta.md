# SeoMarket: развёрнут новый проект Supabase, аудит работает

**Дата:** 15 сентября 2026
**Проект:** `rbkwhejnhiwmktqwkakz` (регион eu-west-1), организация `pmefhpxldimykykmippb`, тариф Pro.

## Что сделано

- **Старый проект Lovable (`dllusofuxonaemrttmwl`) удалён вместе с базой** — подтверждено NXDOMAIN
  у двух независимых DNS. Развернулись заново на восстановленном проекте аккаунта.
- **База поднята с нуля:** 36 миграций, 24 таблицы. Перед этим пришлось:
  - починить три ранние миграции Lovable — они меняли таблицы старой схемы (`projects`,
    `analytics`, `crawl_tasks`, `crawl_results`, `keyword_rankings`), которых нет ни в одной
    миграции и нет в коде; статическая проверка находила 61 такую ошибку, стало 0;
  - переименовать четыре файла в формат `<timestamp>_name.sql` — CLI молча пропускал их,
    а это профили, роли и `has_role`, без которых не встаёт всё остальное;
  - снести шесть пустых таблиц от прежнего эксперимента в этом проекте (0 строк, 0 пользователей).
- **Защита данных на месте с первого дня:** политик `allow_all` — ноль, роли меняет только
  администратор. Дыра, найденная 14.09, в новую базу не попала.
- **25 edge-функций задеплоены.** `health-check`: база и хранилище отвечают.
- **Секреты проекта:** `SERP_PROVIDER`, `XMLRIVER_USER`, `XMLRIVER_KEY`.
- **Приложение переведено на новый проект:** адрес и публичный ключ в `.env`, `project_id`
  в конфиге, сборка проходит.

## Проверено вживую

Запущен аудит `https://zavod-red.ru` со страницы `/site-audit`:

- задача создана в базе, статус `processing`, этап обхода;
- очередь обхода наполняется по-настоящему: **13 719 найденных адресов, 118 обойдено**;
- `positions-check` отвечает «Требуется авторизация» — функция жива и защищена.

## Найдено по ходу

1. **Главная кнопка сайта ведёт на страницу-имитацию.** `/audit` показывает «10 страниц,
   1 критичная ошибка, стоимость 15 600 ₽», **не обращаясь к серверу** — в базе при этом
   ни задачи, ни строки. Настоящий аудит живёт на `/site-audit`. Надо свести на рабочий путь.
2. **«Быстрый аудит до 10 страниц без регистрации» берёт 300 страниц.** На большом сайте это
   надолго и расходует ресурсы проекта.
3. **Кнопка «Войти (Debug)» в шапке** — отладочная, на витрине ей не место.
4. **`crawled_pages` остаётся пустой**, пока очередь наполняется — проверить, доходят ли
   страницы до таблицы, когда обход завершится.

## Что осталось

1. **Ключ Anthropic** (`ANTHROPIC_API_KEY`) — без него ИИ-оптимизация не работает и честно
   об этом говорит. Вписать в секреты проекта.
2. **Проверка позиций** требует входа — нужен аккаунт: зарегистрироваться на сайте и запустить
   проверку, тогда увидим реальные позиции из Яндекса и Google.
3. Пункты 1–3 из находок выше.
4. Деплой на Pages (последний прошёл 6 августа, запуски отменялись) и домен `seomarket.ru`,
   по которому сейчас чужая страница.
5. `RESEND_API_KEY`, если нужны письма.

## Доступы

Токен Supabase, пароль базы и ключ XMLRiver — в `~/.claude/secrets/supabase.env` и
`seomarket.env`, в репозиторий не попадают.
